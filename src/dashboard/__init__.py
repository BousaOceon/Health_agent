"""Flask dashboard — local UI over the SQLite store (home network only).

Phase 1c stands up the day-one Candidates review page + a status page.
Other pages (sub-target detail, goal roll-up, query) land in later phases.
"""
